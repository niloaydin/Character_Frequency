{
  "name": "character-frequency",
  "version": "1.0.2",
  "description": "This is a library that provides character frequency",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --testEnvironment=node --verbose --forceExit --maxWorkers=1",
    "test-watch": "jest --testEnvironment=node --verbose --forceExit --watchAll --maxWorkers=1",
    "test-coverage": "jest --collect-coverage --testEnvironment=node --forceExit --maxWorkers=1",
    "test:coveralls": "jest --coverage && coveralls < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/niloaydin/character-frequency.git"
  },
  "keywords": [
    "Character",
    "Frequency",
    "Array",
    "String"
  ],
  "author": "Nilay Aydin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/niloaydin/character-frequency/issues"
  },
  "homepage": "https://github.com/niloaydin/character-frequency#readme",
  "devDependencies": {
    "coveralls": "^3.1.1",
    "jest": "^27.5.1"
  }
}
